<h1>Coupons</h1>
<ul>
  <% @coupons.each do |c| %>
    <li><%= c.coupon_code %> - <%= c.store %></li>
  <% end %>
</ul>

<ul>
  <% @coupons.each do |c| %>
    <li><%= link_to c.coupon_code, coupon_path(c) %></li>
  <% end %>
</ul>